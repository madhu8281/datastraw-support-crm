# LOCAL DEVELOPMENT
# Leave DATABASE_URL empty to use local SQLite automatically.
# For production, use a Neon PostgreSQL connection string:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

DATABASE_URL=
ADMIN_PASSWORD=
APP_NAME=DataStraw Support
APP_ENV=development
ALLOWED_ORIGINS=*
